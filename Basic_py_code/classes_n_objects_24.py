print("we can create our own datatype")
print("when want to create student datatype with class")

class student:

    def __init__(self, name, major, gpa, is_on_probation): #student datatype
        self.name = name #parameter
        self.major = major #parameter
        self.gpa = gpa     #parameter 
        self.is_on_probation = is_on_probation #parameter

print("SELF is refering to the object")
        
